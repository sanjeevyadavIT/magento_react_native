import React from 'react';
import { View, Text } from 'react-native';

const WishList = ({ navigation }) => (
  <View style={{ flex: 1 }}>
    <Text style={{ fontSize: 24 }}>Developers are working on it!</Text>
  </View>
);

export default WishList;
