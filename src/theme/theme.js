import colors from './colors';
import spacing from './spacing';
import dimens from './dimens';
import typography from './typography';

/**
 * If `statusBar` color defined in colors.js
 * is a light color set key to `dark` else `light`
 */
export default {
  key: 'dark',
  colors,
  spacing,
  dimens,
  typography,
};
